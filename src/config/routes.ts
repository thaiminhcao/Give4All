const routes = {
  home: '/',
  blog: '/blog',
  project: '/project',
  join: '/join-now',
  create_project: '/create-project',
  create_project_form: '/create-project-form'
};

export default routes;
